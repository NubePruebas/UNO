{
  "name": "kroma",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.js",
    "test": "npm --prefix server test",
    "build": "npm --prefix client install --include=dev && npm --prefix server install --include=dev && npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server start"
  }
}
